# ============================================================================
# Shikigami -- Configuration
# ============================================================================
# Copy this file to .env and fill in your values.
#
#   cp .env.example .env
#
# Docker Compose reads .env automatically -- no need to export anything.
#
# After starting the runner with `docker compose up --build`, connect from
# your phone using your desktop's Tailscale IP:
#   http://<tailscale-ip>:8423/docs
#
# Find your Tailscale IP with: tailscale ip -4
# ============================================================================

# --- Cursor API Key (required) ---
# Get your API key from: https://cursor.com/dashboard (Integrations tab)
# This lets the agent authenticate without a browser login.
CURSOR_API_KEY=your_api_key_here

# --- Project folder ---
# Absolute path to the directory containing your project folders.
# The runner scans this directory and lists projects in the app.
#
# Windows example:  C:\Users\YourName\Downloads\_projects
# macOS example:    /Users/yourname/projects
# Linux example:    /home/yourname/projects
PROJECT_ROOT=~/Downloads/_projects

# --- Port ---
# Port the runner listens on.
# Access from your phone at http://<tailscale-ip>:<port>/docs
RUNNER_PORT=8423
